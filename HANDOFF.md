# 已確認示意稿 V6 — 接續工作入口

## 狀態

使用者已確認 V6（2026-09-15 工作階段）：「這樣對了」。此次只交付示意稿與交接檔；正式網站 index.html、content.js、ui.css、runtime.js 尚未改版，影片效能優化尚未實作。

## 第一優先：跨設備角色影片卡住（使用者 2026-09-16 補充）

使用者回報上版本 3D 角色影片換設備容易卡住，要求優先解決。尚未重現或定位根因；原正式網站使用男女管理員影片，佩麗卡目前僅擷取静態畫格，不應把原版測試當作佩麗卡播放已驗證。

- 先詢問出問題的設備、瀏覽器、網路與發生時機，同時自主檢查現有播放器、影片編碼/碼率/解析度、WebGL 合成、自動播放及背景恢復流程。
- 優先建立佩麗卡實際影片播放測試；量測首格時間、播放時間是否前進、合成畫格是否更新，分辨載入卡住、解碼卡住或合成停止。不能只靠截圖或「有 video 元素」判定成功。
- 靜態備援先呈現；第一格完成後再顯示影片。處理載入逾時、播放拒絕、持續無新畫格與 WebGL context lost，採有上限的恢復/重試，失敗時平順退回靜態，避免無限重試或定格冒充正常播放。
- 覆蓋冷快取、慢網路、切分頁/鎖屏後返回、角色輪替、長時間循環；記錄實際測試裝置及限制。至少桌機 Chromium，能取得設備時測 iPhone Safari 與 Android Chrome；模擬不能宣稱實機通過。
- 在不卡住、備援可靠的基礎上再優化轉場與其他素材；保持 V6 已確認構圖。不要只把影片停用作為最終修復。

## 已確認設計（請勿退回舊版）

- 雙 Logo：終末地在左、明日方舟在右。明日方舟已縮小約 23%；桌機 148 × 72、手機 80 × 38 CSS px，以原始比例顯示。
- 手機雙 Logo 靠左，「現場叫號看板」靠右；刪去原先明日方舟：終末地文字副標。
- 佩麗卡：官方 3D 待機影片半身，人物比最初版本上移。
- 阿米婭：彩色前景偏右下，後景放大頭肩偏左上，清楚露出後景臉，不能兩層齊下。黑底參考圖只參考構圖，**不要黑色色塊**。
- 阿米婭區域白底，沒有黃色人物直帶、沒有 ENDFIELD 背景字；有淡 RHODES ISLAND 字。
- 後景使用官方透明 amiya_e1 立繪，27% 不透明度，四周及下方柔和漸隱，不能有明顯矩形切線。
- 前景向右、後景向左慢速微移；桌機 14/20px，手機 5/7px；6.5 秒單程、往返。示意稿已實作；不是正式網站功能。
- 號碼、DAY、公告需維持清楚。手機耳尖可輕微蓋到叫號面板下緣，不遮資訊。

## 本機檔案

- `mockups/proposal.css`：目前 V6 精確樣式（累積覆寫，正式實作時整理）。
- `mockups/build.cjs`：產生示意 HTML；引用原網站參考副本，不修改正式入口。
- `mockups/*-v6.png`：已確認的四張完整圖片。
- `mockups/index.html`：比較頁。
- `mockups/render-check.json`：四版圖片載入、無 JS 錯誤及無水平溢出均通過。
- `reference-assets/`：下載的官方原始素材。

GitHub `handoff-v6/previews/` 的 SVG 包裝 JPEG 是方便預覽的示意圖，不是人物向量素材。正式實作使用官方 PNG/MP4/SVG，不要用示意截圖切圖。

## 明天換電腦

1. 下載此 GitHub 倉庫，先讀本檔、README.md、SOURCES.md。
2. 如無本機交接 ZIP，可執行 `powershell -ExecutionPolicy Bypass -File handoff-v6/restore.ps1`，重建示意稿資料夾與官方素材（需 Node.js、npm、網路、Edge）。
3. 執行 `node preview.cjs 8080`。
4. 靜態示意：`http://127.0.0.1:8080/mockups/proposal.html?motion=off&character=female&view=mobile`。
5. 微動態：在上述網址加 `&demo=on`。這個 demo 獨立於原站 motion 開關；不要直接照搬正式網站。
6. 執行 `node mockups/render.cjs` 可重製四張 PNG。

## 下一段：正式實作及素材效能

1. 先同步 GitHub 最新版本，優先按上方「跨設備角色影片卡住」段落定位及驗證，再移植已確認版型；保留 900px 斷點與號碼光學置中。
2. 將男女管理員替換成佩麗卡影片與阿米婭雙圖片圖層。
3. 佩麗卡原片左右為 RGB 與遮罩，合成後 1920 × 1080；示意圖擷取 1 秒畫格。製作手機/桌機合適解析度及碼率版本，保留原片。
4. 首屏先顯示靜態備援；影片第一格合成完成才切入，圖片 decode 完成才輪替。避免切換當下才開始載入。
5. 減少動態、背景分頁、載入失敗需妥善停止/降級；避免無意同時播放多支影片。阿米婭只用圖片 transform，不增加解碼影片。
6. 原 Noto Sans TC 完整 WOFF2 約 5.4 MB，可研究分包；不要只留下目前公告字而造成新公告缺字。
7. 執行原 verify.cjs 並更新因角色變更失效的測試；實測手機、桌機、輪替、失敗備援與 reduced-motion。Safari/手機實機尚未驗證。

## 注意

使用者先核准示意再改網站。本階段僅交接；不要把此次上傳描述成正式網站改版或效能優化已完成。

